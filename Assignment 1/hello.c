#include<stdio.h>
int main(){  // for creating folder enter (mkdir hello.c)
    printf("Tanush Saha"); //  (vi hello.c)==> Entering into file ...Then enter code ..
    printf("Welcome to my class"); //for save (esc,:,w,q)
    return 0; //Then again enter (ls), Then enter (gcc hello.c).. for output enter(./a.out)
}